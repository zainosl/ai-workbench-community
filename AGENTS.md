# AI 工作台的 AI 配置指南

本仓库是可公开分享的通用源码，不是用户私人资料库。Codex、WorkBuddy 或其他 AI 在此目录工作时，先读 `README.md` 与 `Resources/Templates/*.md`。

## 首次设置

1. 询问用户的真实业务/项目背景，但不要要求一次提供全部信息。优先明确“用户在什么场景下要完成什么任务”，再讨论方案、单元模型、增长、壁垒。
2. 查看 `~/Library/Application Support/AIWorkbenchCommunity/` 是否已有配置和资料。仅在该本机目录写入用户资料，不要写入此 Git 仓库。
3. 沿用模板标题/表格/假设编号格式，以便解析器工作。给出简短预览，指出哪些是真实证据，哪些只是判断或待验证假设。
4. 经用户确认后，在本机 `config.json` 设置 `configured: true`。如用户已有项目资料，可按 README 的 `projects` 结构配置；不要擅自扫描无关目录。
5. 运行 `swift run AIWorkbenchCoreSelfTest` 与 `./scripts/install.sh`。安装脚本使用独立的开源版名称，不改动其他工作台。

## 后续迭代

- 保留读取、执行、学习、沉淀及经营/假设分域更新的框架；先解释变动的判断逻辑，再改代码。
- 不把 AI 的资料整理、任务完成、客户口头兴趣直接算成商业假设成立。
- 核对已有假设时先找证伪与削弱，再找支持；新增假设进入观察池，不自动改写关键假设库。
- 经营取舍、证据采纳、对外发送和发布需要用户确认。
- 提交代码前检查硬编码姓名、客户、绝对私人路径、密钥、任务深度链接、截图与日志。

请用简洁、可操作的中文引导用户；未知的信息标“待填写”，不要编造。
